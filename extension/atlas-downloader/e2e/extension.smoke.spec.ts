import { test } from '@playwright/test';

test.describe('atlas extension smoke', () => {
  test.skip('configure extension + verify content filtering flow', async () => {
    // Placeholder for extension-level browser flow.
    // Add extension launch fixture + options/content assertions in follow-up.
  });
});
