# Extension E2E

This folder is reserved for extension-specific Playwright scenarios.

Current status:
- `extension.smoke.spec.ts` is a scaffold and intentionally skipped.

Recommended next step:
- Add an extension-launch fixture and cover options -> content filtering -> reaction flow in-browser.
