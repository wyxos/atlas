# reverb-service.yaml - Laravel Reverb service for Atlas
apiVersion: v1
kind: Service
metadata:
  name: atlas-reverb
  namespace: apps
spec:
  selector:
    app: atlas-reverb
  ports:
    - port: 8080
      targetPort: 8080