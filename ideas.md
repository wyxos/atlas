Extension
- image below set width > anti container CTA
- default width 300
- anti container allow bypass and react directly (for situations where there's no container and image/video is legitimately small)
