model = "gpt-5.3-codex"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Maintain concise, actionable workflow documentation.
Update AGENTS/runbook docs only when a reusable, objective guideline or recurring gotcha is discovered.
Avoid duplicates and keep entries short.
"""